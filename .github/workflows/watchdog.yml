name: Watchdog
on:
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *'
jobs:
  watch:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@v4
      - run: python runtime/watchdog.py
